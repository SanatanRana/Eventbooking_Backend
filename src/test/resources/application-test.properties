spring.application.name=ranadj

# Database details for testing (dummy credentials so context can load)
spring.datasource.url=jdbc:mysql://localhost:3306/testdb
spring.datasource.username=test
spring.datasource.password=test

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

jwt.secret=testsecretkeytestsecretkeytestsecretkey123
jwt.expiration=86400000

# Razorpay dummy test values
razorpay.key.id=test_key
razorpay.key.secret=test_secret

# Azure dummy test values
azure.storage.connection-string=DefaultEndpointsProtocol=https;AccountName=test;AccountKey=dGVzdA==;EndpointSuffix=core.windows.net
azure.storage.container-name=test-container

# File Upload Size Limits
spring.servlet.multipart.max-file-size=250MB
spring.servlet.multipart.max-request-size=250MB
